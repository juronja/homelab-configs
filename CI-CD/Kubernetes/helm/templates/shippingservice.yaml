apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.shippingService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.shippingService.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.shippingService.name }}
    spec:
      containers:
      - name: {{ .Values.shippingService.name }}
        image: us-central1-docker.pkg.dev/google-samples/microservices-demo/{{ .Values.shippingService.name }}:v0.10.2
        ports:
        - containerPort: {{ .Values.shippingService.port }}
        env:
        - name: PORT
          value: "50051"
        readinessProbe:
          periodSeconds: 5
          grpc:
            port: {{ .Values.shippingService.port }}
        livenessProbe:
          periodSeconds: 5
          grpc:
            port: {{ .Values.shippingService.port }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.shippingService.name }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.shippingService.name }}
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.shippingService.port }}
      targetPort: {{ .Values.shippingService.port }}