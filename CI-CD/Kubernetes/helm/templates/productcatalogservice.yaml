apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.productCatalogService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.productCatalogService.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.productCatalogService.name }}
    spec:
      containers:
      - name: {{ .Values.productCatalogService.name }}
        image: us-central1-docker.pkg.dev/google-samples/microservices-demo/{{ .Values.productCatalogService.name }}:v0.10.2
        ports:
        - containerPort: {{ .Values.productCatalogService.port }}
        env:
        - name: PORT
          value: "3550"
        readinessProbe:
          grpc:
            port: {{ .Values.productCatalogService.port }}
        livenessProbe:
          grpc:
            port: {{ .Values.productCatalogService.port }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.productCatalogService.name }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.productCatalogService.name }}
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.productCatalogService.port }}
      targetPort: {{ .Values.productCatalogService.port }}