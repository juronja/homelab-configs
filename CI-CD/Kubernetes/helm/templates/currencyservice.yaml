apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.currencyService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.currencyService.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.currencyService.name }}
    spec:
      containers:
      - name: {{ .Values.currencyService.name }}
        image: us-central1-docker.pkg.dev/google-samples/microservices-demo/{{ .Values.currencyService.name }}:v0.10.2
        ports:
        - containerPort: {{ .Values.currencyService.port }}
        env:
        - name: PORT
          value: "7000"
        - name: DISABLE_PROFILER
          value: "1"
        readinessProbe:
          grpc:
            port: {{ .Values.currencyService.port }}
        livenessProbe:
          grpc:
            port: {{ .Values.currencyService.port }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.currencyService.name }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.currencyService.name }}
  type: ClusterIP
  ports:
    - protocol: TCP
      port: {{ .Values.currencyService.port }}
      targetPort: {{ .Values.currencyService.port }}