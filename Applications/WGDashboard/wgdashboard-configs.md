# WGDashboard configurations

## Peer settings

DNS<br />
`use gateway router` (with DNS records)

Endpoint Allowed IPs<br />
`0.0.0.0/0,192.168.X.0/24` (home network CIDR)

Peer Remote Endpoint<br />
`location.domain.com`